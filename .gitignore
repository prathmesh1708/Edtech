# Dependencies
node_modules/

# Environment files — NEVER commit real secrets, but never lose them either.
# Keep a copy of your real .env outside the repo (see SETUP.md).
.env
.env.local
.env.*.local
!.env.example

# Build output
dist/
dist-ssr/
build/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS / editor junk
.DS_Store
.idea/
*.swp

# Misc local scratch files
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
